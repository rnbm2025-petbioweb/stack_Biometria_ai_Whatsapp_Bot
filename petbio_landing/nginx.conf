# ============================================
# 🔧 Archivo principal de configuración NGINX
# Proyecto: PETBIO Web Gateway
# ============================================

user  www-data;
worker_processes  auto;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;

# ============================================
# ⚙️ BLOQUE DE EVENTOS
# ============================================
events {
    worker_connections 1024;
    multi_accept on;
}

# ============================================
# 🌐 BLOQUE HTTP GLOBAL
# ============================================
http {
    include       mime.types;
    default_type  application/octet-stream;

    # ============================================
    # 🔁 Definición de upstreams (servicios internos)
    # ============================================
    upstream php {
        server 127.0.0.1:9000;
        keepalive 8;
    }

    # Logs globales
    access_log  /var/log/nginx/access.log;
    error_log   /var/log/nginx/error.log;

    # Performance
    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout  65;
    types_hash_max_size 2048;

    # Seguridad
    server_tokens off;

    # Compresión Gzip
    gzip on;
    gzip_disable "msie6";
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 5;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Headers globales de seguridad
    include /etc/nginx/security-headers.conf;

    # Tamaño máximo de subida global
    client_max_body_size 250M;

    # ============================================
    # 🧩 Incluir todos los virtual hosts definidos
    # ============================================
    include /etc/nginx/conf.d/*.conf;
}
        
